# Component Conventions

> Заповнити при старті `packages/web`. Поки стек не визначено — це шаблон.

## Naming

- [ ] File naming: `PascalCase.tsx` чи `kebab-case.tsx`?
- [ ] Component vs container розмежування?

## Structure

- [ ] Де живуть shared UI компоненти?
- [ ] Де живуть page-level компоненти?
- [ ] Чи є design system / UI бібліотека?

## Props

- [ ] Props typing: окремий `type Props = {...}` чи inline?
- [ ] Чи використовуємо `children` prop явно?

## State Management

- [ ] Server state: React Query / SWR / інше?
- [ ] Client state: Context / Zustand / інше?
